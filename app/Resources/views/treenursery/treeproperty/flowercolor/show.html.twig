{% extends 'base.html.twig' %}

{% block body %}
    <h1>Flowercolor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flowerColor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ flowerColor.name }}</td>
            </tr>
            <tr>
                <th>Deletedat</th>
                <td>{% if flowerColor.deletedAt %}{{ flowerColor.deletedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('treenursery_flower-colors_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('treenursery_flower-colors_edit', { 'id': flowerColor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
